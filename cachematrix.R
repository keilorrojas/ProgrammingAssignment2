## Assignment2 didn't have time enough to solve it 


m<- makeCacheMatrix()
m$set(matrix(c(0,2,2,0),2,2))
      m$getmean()
      cacheSolve(m)
      cacheSolve(m)
      m$get()
      m$set(matrix(c(1,0,1,0),2,2))
      m$getmean()
      cacheSolve(m)
      cacheSolve(m)
      m$get()
